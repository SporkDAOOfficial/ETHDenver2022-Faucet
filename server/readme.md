#### Installation

(all from ./server)

`yarn install`


#### Setup

1. `cp .env.sample .env`
1. Update variables to appropriate values in .env

#### Run

`yarn start`

Visit `http://localhost:<port>/ping`

